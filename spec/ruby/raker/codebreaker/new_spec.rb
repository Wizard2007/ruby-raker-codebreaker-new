require "spec_helper"

describe Ruby::Raker::Codebreaker::New do
  it "has a version number" do
    expect(Ruby::Raker::Codebreaker::New::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
